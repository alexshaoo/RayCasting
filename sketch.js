let height = 400;
let width = 400;

let boundary;

function setup() {

    createCanvas(width, height);
    boundary = new Boundary(300, 100, 300, 300);
}

function draw() {
    background(0);
    b.show();
}